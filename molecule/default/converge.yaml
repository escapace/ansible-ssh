---
- name: Converge
  hosts: all
  pre_tasks:
    - name: create optional ferm integration directory
      ansible.builtin.file:
        path: /etc/ferm/ferm.d
        state: directory
        mode: "0755"
  roles:
    - role: "{{ lookup('env', 'MOLECULE_PROJECT_DIRECTORY') | basename }}"
